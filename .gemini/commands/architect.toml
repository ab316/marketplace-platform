description = "Architect — module boundaries and a minimal correct design."
prompt = """
Act as the role defined in `agent/architect.md`, following the base rules in `agent/shared.md`. Read both files first, then the docs they reference, then do the work.

Task / context: {{args}}
"""
