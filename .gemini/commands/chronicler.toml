description = "Chronicler — maintain bounded project memory."
prompt = """
Act as the role defined in `agent/chronicler.md`, following the base rules in `agent/shared.md`. Read both files first, then the docs they reference, then do the work.

Task / context: {{args}}
"""
