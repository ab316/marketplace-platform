---
description: QA — destructive test matrix and exit criteria.
argument-hint: [task or context]
---

Act as the role defined in `agent/qa.md`, following the base rules in `agent/shared.md`. Read both files first, then the docs they reference, then do the work.

Task / context: $ARGUMENTS
