# Worklog (Atomic History)

One file per merged PR or significant completion event.

Path pattern:

`docs/ops/worklog/YYYY/YYYY-MM-DD-pr-<id>.md`

Use `TEMPLATE.md` for consistency.

Retention:

- Keep all entries (append-only)
- Do not rewrite historical facts; add correction notes if needed
